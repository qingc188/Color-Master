# 更新日志

## 2026-08-21

### 颜色算法 V3

- 颜色复现评分在 Oklab 基础色差上增加平滑的低饱和度修正，降低“有颜色的目标被还原成灰色”时的虚高分数。
- 颜色匹配继续使用原始 Oklab 距离，色板难度和干扰色生成不受复现评分调整影响。
- 复现结果增加色相、饱和度和明度方向反馈，不再向普通用户展示“彩度”术语。
- 第三版复现最佳成绩使用 `_oklab_v3` 存储键，不与第二版成绩混用。
- 增加中性色修正、截图案例排序、默认灰色基线、对称性和满分恒等性的回归测试。

## 2026-08-19

### 颜色算法 V2

- 将颜色复现评分从伽马编码 sRGB 欧氏距离改为 Oklab 感知色差。
- 将色差计算与游戏得分映射分离，使用明确的分段锚点将 Oklab 色差转换为 0～10 分。
- 分数改为整数百分位累计，避免每轮提前格式化和浮点累加造成总分误差。
- 复现结果增加 `/ 10` 和简短的接近程度反馈，并通过评分说明弹窗按需展示 Oklab 色差、评分区间、公式与锚点。
- 新版复现最佳成绩使用 `_oklab_v2` 存储键，不与旧 RGB 评分记录混用。

### 颜色匹配

- 目标色改为在 Oklch 中生成，并根据当前亮度和色相的 sRGB 色域上限选择彩度。
- 干扰色改为按难度和关卡对应的 Oklab 感知距离带生成。
- 增加干扰色之间的最小感知距离，避免多个选项挤在同一视觉区域。
- 选项唯一性改为按量化后的 RGB/HEX 判断，避免 HSL 字符串不同但屏幕颜色相同。
- 干扰色生成改为有次数上限的整组生成，不再使用无上限 `while` 循环。
- 增加匹配回合提交锁，避免一次选择重复计分或重复扣除生命。

### 修复与测试

- 复现控制区域显示后重新计算 HSL 色轮指针位置，避免隐藏元素尺寸为零导致初始位置错误。
- 基础复现增加原生色相与饱和度滑杆，并与色轮双向同步；目标会规范为控件可精确表达的整数 HSL，确保能够通过实际控件获得满分。
- 新增 `tests/color-utils.test.js`，使用 Node.js 内置测试运行器，无需安装依赖。
- 新增 `tests/browser-regression.js`，通过 Edge 实际验证匹配、复现和移动端关键路径。
- 测试覆盖 Oklab 官方参考向量、RGB 往返、评分锚点、单调性、RGB 唯一性和各难度色板距离。

### 复现界面

- 将复现滑杆调整为 6px 可见轨道、17px 滑块和 40px 交互高度，并保留键盘焦点反馈。
- 基础模式增加精确的色相与饱和度滑杆，与 HSL 色轮双向同步。
- 手机竖屏采用色轮与滑杆并排的紧凑工作台，当前复现改为横向色条，默认文字大小下可在首屏提交。
- 复现阶段使用独立轮次提示，切换到控制区和结果区时会将键盘焦点移到对应标题。
- 增加稳定轮换的分数情绪反馈，以及可通过关闭按钮、Escape 和背景关闭的页面内评分说明弹窗。
- 颜色复现的观察、调整和本轮结果阶段增加当前轮次、累计得分与当前难度最佳统计栏；提交后立即刷新累计得分。
- 两种模式的准备界面统一隐藏进行中统计，并移除复现调整页内重复的轮次提示。
- 将颜色复现统计栏移动到复现容器内部，与观察、调整或本轮结果组成单一连续工作台；桌面基础模式改为横向色轮控制以降低整体高度。

### 首页与色卡

- 首页移除重复的“了解玩法”按钮和游戏技巧栏，以“开始挑战”作为唯一主操作。
- 将色彩历史改为独立的“我的色卡”页面，增加收录计数、空状态和轻量首页入口。
- 颜色匹配与颜色复现的目标观察时间统一从 6 秒调整为 5 秒。
- 将圆形色点重构为方形色样档案盘，以固定列宽和补齐当前十格行的空槽改善宽屏与手机排版。
- 色卡上限从 50 提升到 100，最近收录的颜色优先显示，并增加固定的 HEX、RGB、HSL 与来源详情区。
- 将清空操作移到档案底部并增加同页二次确认，降低误触风险。

### 小红书离线兼容

- 将 Tailwind 从运行时 CDN 改为预编译的本地静态样式，移除入口页内联配置脚本。
- 移除 Font Awesome 外部样式与字体依赖，使用页面内 SVG 图标符号替代。
- 浏览器回归默认阻断所有 HTTP/HTTPS 页面请求，并验证本地样式和图标加载。
- 新增小红书小工具合规测试，检查外链资源、内联脚本和容器禁用 API。
- 为 LocalStorage 读写和删除增加安全降级；存储不可用时保留本局功能，并在页面显示记录无法保存的状态。
- 当前版本暂不提供 BGM，移除音量控制和喇叭图标；答题反馈继续使用可静默降级的 Web Audio 短提示音。
- 将评分说明改为带焦点循环和焦点归还的页面内模态框，减少原生 `dialog` 在 WebView 中的兼容风险。
- 色卡固定详情区使用 `textContent` 渲染持久化内容，避免将历史数据写入 `innerHTML`。

### 设计说明

- Oklab 现在同时用于游戏目标生成、匹配难度控制和复现评分，使两种模式采用一致的感知距离口径。
- CIEDE2000 暂未加入运行时代码。若未来需要面向印刷或专业质检的标准化评分，可在保留 Oklab 色板生成的基础上增加 CIEDE2000 评分模式。
